import 'package:flutter/material.dart';

class SubjectiveFormScreen extends StatelessWidget {
  const SubjectiveFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Subjective Form')),
      body: Center(child: Text('Form Here')),
    );
  }
}
